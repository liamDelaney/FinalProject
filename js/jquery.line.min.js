/*-------------------------------------------------------------------------------------------------
  This plugin is based on the GAPJUMPER line example http://www.gapjumper.com/research/lines.html.
  Special thanks to its author!
  Author: Tiago do Bem
  Date: March 2013
  URL: https://github.com/tbem/jquery.line
  The jQuery.line.js plugin is distributed under the GNU General Public License version 3 (GPLv3).
  -------------------------------------------------------------------------------------------------
*/
!(function (t) {
  const e = function (t, e, s, l, a) {
    const n = navigator.userAgent.indexOf('MSIE') > -1; if (s < t) { let r = t; t = s, s = r, r = e, e = l, l = r; } const o = document.createElement('div'); o.className = a.class; const i = Math.sqrt((t - s) * (t - s) + (e - l) * (e - l)); if (o.style.width = `${i}px`, o.style.borderBottom = `${a.stroke}px ${a.style}`, o.style.borderColor = a.color, o.style.position = 'absolute', o.style.zIndex = a.zindex, n) {
      o.style.top = l > e ? `${e}px` : `${l}px`, o.style.left = `${t}px`; let c = (s - t) / i,
        f = (l - e) / i; o.style.filter = `progid:DXImageTransform.Microsoft.Matrix(sizingMethod='auto expand', M11=${c}, M12=${-1 * f}, M21=${f}, M22=${c})`;
    } else { const d = Math.atan((l - e) / (s - t)); o.style.top = `${e + 0.5 * i * Math.sin(d)}px`, o.style.left = `${t - 0.5 * i * (1 - Math.cos(d))}px`, o.style.transform = o.style.MozTransform = o.style.WebkitTransform = o.style.msTransform = o.style.OTransform = `rotate(${d}rad)`; } return o;
  }; t.fn.line = function (s, l, a, n, r, o) { return t(this).each(function () { t.isFunction(r) ? (callback = r, r = null) : callback = o, r = t.extend({}, t.fn.line.defaults, r), t(this).append(e(s, l, a, n, r)).promise().done(() => { t.isFunction(callback) && callback.call(); }); }); }, t.fn.line.defaults = {
    zindex: 1e4, color: '#000000', stroke: '1', style: 'solid', class: 'line',
  };
}(jQuery));
